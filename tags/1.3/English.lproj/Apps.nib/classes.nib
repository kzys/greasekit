{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {addApplication = id; removeApplication = id; }; 
            CLASS = "Info8_pGKAppsController"; 
            LANGUAGE = ObjC; 
            OUTLETS = {applicationTableView = NSTableView; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}