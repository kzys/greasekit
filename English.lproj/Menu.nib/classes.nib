{
    IBClasses = (
        {
            ACTIONS = {
                orderFrontAboutPanel = id; 
                reloadUserScripts = id; 
                toggleScriptEnable = id; 
                uninstallSelected = id; 
            }; 
            CLASS = CMController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                applicationsButton = NSPopUpButton; 
                scriptsController = NSArrayController; 
                topMenu = NSMenu; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}